{
  "name": "finance-guru-desktop",
  "version": "0.1.0",
  "description": "Desktop GUI for Finance Guru analysis engine",
  "main": "main.js",
  "scripts": {
    "start": "bun scripts/build-renderer.js && electron .",
    "start:dev": "bun scripts/build-renderer.js && electron . --dev",
    "watch": "bun scripts/build-renderer.js --watch",
    "build:renderer": "bun scripts/build-renderer.js",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "author": "AojDevStudio",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@anthropic-ai/claude-code": "^1.0.0",
    "marked": "^17.0.3",
    "plotly.js-dist-min": "^2.35.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "esbuild": "^0.27.2",
    "@happy-dom/global-registrator": "^20.0.10"
  }
}
